# Dipendenze dirette — PIN ESATTI, nessun range aperto (ESP §0, IC §1.3, C-1).
#
# esper NON è qui: è VENDORIZZATO in vendor/esper/ (pinnato 3.7), non risolto da
# un indice esterno. Vedi vendor/esper/VENDOR.md.
#
# Il game engine è HEADLESS: nessuna libreria di UI tra le dipendenze. Una
# presentazione futura (web, Electron, TUI…) porterà le proprie dipendenze nel
# proprio strato, fuori dal motore.
#
# Il lockfile con la chiusura transitiva pinnata è requirements.lock.

pydantic==2.10.6
